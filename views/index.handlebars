
    <h2>Hello World!</h2>
    <p>Welcome to my website</p>

    {{#if isDisplayName}}
        <p>My name is {{ name }}</p>

        {{else}}
        <p>No name found!</p>
    {{/if}}


    {{#if isAgeEnabled}}
    <p>I am {{ age }} years old</p>
        {{else}}
        <p>No age provided</p>
    {{/if}}

